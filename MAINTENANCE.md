# 運用・メンテナンスガイド (SKY-MAGYCC JUDAS)

このドキュメントは、ゲームの機能更新やバグ修正を行った後の「リリース・運用フロー」をまとめた自分用マニュアルです。

---

## 1. アップデートの流れ (基本ステップ)

機能の追加や修正が完了したら、以下の手順で各プラットフォームに反映します。

### ステップ1：バージョンの更新
サーバーとアプリのバージョンを一致させます。不一致だとユーザーが接続時にエラー画面になります。

1.  **サーバー側**: `nexus-war-server/package.json` の `"version"` をインクリメント。
2.  **アプリ側**: `nexus-war-app/package.json` の `"version"` をサーバーと同じ数値に更新。

### ステップ2：サーバーのデプロイ (Render)
1.  修正内容を `main` ブランチにプッシュ（マージ）します。
2.  Render のダッシュボードを確認し、自動デプロイが完了するのを待ちます。

### ステップ3：アプリの配布 (itch.io)
ターミナル（`nexus-war-app` ディレクトリ）で以下のコマンドを実行します。
```bash
# PATHが通っていない場合のみ（必要に応じて）
$env:PATH += ";$env:USERPROFILE\.cargo\bin"

# ビルド ＆ アップロード実行
npm run deploy:itch
```
これで、Windows版（インストーラー）と Web版（ブラウザ版）が同時に itch.io へ送信されます。

---

## 2. 運用上の注意点

### サーバーのスリープ対応 (Render 無料枠)
Render の無料枠は一定時間アクセスがないとスリープします。
*   **症状**: ゲーム開始時に「サーバーに接続中...」が長く続く。
*   **対策**: 公開ページの説明文に「サーバーの起動に 1 分ほどかかる場合があります」と注釈を入れるか、有料プラン（Starters: $7/mo）への変更を検討してください。

### データのバックアップ
現在は静的な JSON ファイル（`src/data/*.ts`）でデータを管理しています。
*   写真やブログ記事を追加した際は、必ず `main` ブランチへプッシュして GitHub 上に正解データを残してください。

---

## 3. トラブルシューティング

*   **`butler login` が必要と言われたら**:
    `C:\butler\butler.exe login` を実行して再認証してください。
*   **バージョン不一致エラーが出たら**:
    サーバー側の `package.json` とクライアント側の `package.json` のバージョン番号が完全に一致しているか確認してください。
*   **ビルドが失敗する**:
    `$env:PATH += ";$env:USERPROFILE\.cargo\bin"` で Rust のパスが通っているか確認してください。
